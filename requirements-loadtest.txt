# Load testing dependencies — install separately:
#   pip install -r requirements-loadtest.txt
#
# Run (interactive UI):
#   locust -f load_tests/locustfile.py --host http://localhost:8000
#
# Run (headless, 20 users, 60s):
#   locust -f load_tests/locustfile.py --host http://localhost:8000 \
#     --headless -u 20 -r 5 --run-time 60s
#
# Target a different API key:
#   LOCUST_API_KEY=my-prod-key locust -f load_tests/locustfile.py ...

locust==2.32.3
